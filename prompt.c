#include "shell.h"
/**
 * prompt - to display the described prompt.
 * Return: void(no return).
 * Description: this function will display a prompt to ask user for input.
 */
void prompt(void)
{
	_printf("$$ ");
}
